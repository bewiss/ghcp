# Copy to .env and fill in your real tokens (never commit real secrets)

# Provider can be 'github' (GitHub Models) or 'openai'. Default is 'github'.
# If you only have an OpenAI key, set PROVIDER=openai.
# PROVIDER=github

# When using GitHub Models (Copilot): requires eligible Copilot access
# GITHUB_TOKEN=ghp_yourPersonalAccessTokenHere

# When using OpenAI:
# PROVIDER=openai
# OPENAI_API_KEY=sk-your-openai-key
